// Core
import { lazy } from 'react';

import { option__pageName__(pascalCase) } from './option__pageName__(pascalCase)';

export const __pageName__(pascalCase) = {
    ComponentLazy: lazy(() => import(/* webpackChunk__pageName__(pascalCase): "__pageName__(pascalCase)" */ './ComponentMarkdown')),
    option:        option__pageName__(pascalCase),
};
