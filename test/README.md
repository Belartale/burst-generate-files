### The folder `generate` is configurations

### The folder `src` is the folder for testing the package

### Test with development mode

`npm run test:dev`

### Test with production mode

`npm run test:build`